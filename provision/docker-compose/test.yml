# Docker Compose
# https://docs.docker.com/compose/
#
# Usage: test up

version: "3.6"
services:

  app:
    image: pyfacebook-app-test
    environment: &environment
      - STAGE=test
    build:
      args:
        stage: test
    volumes:
      - .:/usr/src
